# 🧪 ТЕСТИРОВАНИЕ РАНДОМИЗАЦИИ КАРТ ТАРО

## 🚀 Быстрый запуск

### Вариант 1: В консоли браузера

1. Запустите dev server:
```bash
npm run dev
```

2. Откройте браузер и консоль разработчика (F12)

3. Импортируйте и запустите тест:
```javascript
// После загрузки приложения
import { testSimpleReading } from './src/utils/test-randomization-browser';
testSimpleReading(3);
```

### Вариант 2: В React компоненте

Создайте временный компонент для тестирования:

```tsx
import { useEffect } from 'react';
import { tarotDeck } from '@/data/tarot-deck';
import { createReading } from '@/utils/tarot-randomization';
import { formatReadingSimple } from '@/utils/format-reading';
import type { SpreadType } from '@/types';

function TestRandomization() {
  useEffect(() => {
    const testSpread: SpreadType = {
      id: 1,
      name: 'Test',
      nameRu: 'Тест',
      description: '',
      cardCount: 3,
      category: 'general',
      positions: [
        { position: 1, name: 'Pos 1', nameRu: 'Поз 1', meaning: '' },
        { position: 2, name: 'Pos 2', nameRu: 'Поз 2', meaning: '' },
        { position: 3, name: 'Pos 3', nameRu: 'Поз 3', meaning: '' },
      ],
      isPremium: false,
      icon: '🎴',
    };

    const reading = createReading(tarotDeck, testSpread);
    console.log('Результат расклада:');
    console.log(formatReadingSimple(reading.cards));
  }, []);

  return <div>Проверьте консоль браузера</div>;
}
```

## 📋 Формат вывода

Результат будет в следующем формате:

```
Card ID: 42 | Положение: вертикальное
Card ID: 15 | Положение: перевёрнутое
Card ID: 67 | Положение: вертикальное
```

Где:
- **Card ID** - уникальный идентификатор карты (1-78)
- **Положение** - "вертикальное" или "перевёрнутое"

## ✅ Что проверяется

1. ✅ Загрузка JSON данных (78 карт)
2. ✅ Рандомизация колоды
3. ✅ Выбор уникальных карт
4. ✅ Определение переворота (50/50)
5. ✅ Форматирование вывода

## 📊 Пример результата

```
🎴 === ТЕСТ РАНДОМИЗАЦИИ КАРТ ТАРО ===

📊 Статистика колоды:
  - Всего карт: 78
  - Старшие Арканы: 22
  - Младшие Арканы: 56
  - Жезлы: 14, Кубки: 14, Мечи: 14, Пентакли: 14

✅ Расклад создан успешно (3 карты)

📋 Результат в формате: Card ID | Положение
──────────────────────────────────────────────────
Card ID: 42 | Положение: вертикальное
Card ID: 15 | Положение: перевёрнутое
Card ID: 67 | Положение: вертикальное
──────────────────────────────────────────────────

✅ Тест завершён успешно!
```

## 🔧 Доступные функции

### Простой тест
```typescript
import { testSimpleReading } from '@/utils/test-randomization-browser';
testSimpleReading(3); // 3 карты
```

### Подробный тест
```typescript
import { logDetailedReading } from '@/utils/test-randomization-browser';
logDetailedReading(5); // 5 карт с подробной информацией
```

### Несколько раскладов
```typescript
import { testMultipleReadings } from '@/utils/test-randomization';
const result = testMultipleReadings(5, 3); // 5 раскладов по 3 карты
```

### Все тесты
```typescript
import { runAllTests } from '@/utils/test-randomization';
const result = runAllTests();
```

## 📝 Файлы для тестирования

- `src/test-randomization.ts` - простой запуск тестов
- `src/utils/test-randomization.ts` - полные тесты
- `src/utils/test-randomization-browser.ts` - тесты для браузера
- `src/examples/test-randomization-example.ts` - примеры использования

## 🐛 Решение проблем

### Колода не загружается
```typescript
import { validateDeck } from '@/data/tarot-deck';
console.log('Валидна:', validateDeck()); // должно быть true
```

### Crypto API недоступен
```typescript
import { isCryptoAvailable } from '@/utils/randomization';
console.log('Crypto доступен:', isCryptoAvailable()); // должно быть true
```

### Ошибка при создании расклада
Проверьте, что:
1. Колода загружена (78 карт)
2. SpreadType корректно настроен
3. cardCount не превышает 78

## 📚 Дополнительная документация

- `src/utils/TESTING.md` - подробная документация по тестированию
- `src/utils/README.md` - документация утилит
- `docs/randomization-system.md` - описание системы рандомизации

