# 📱 Руководство по деплою Telegram Mini App через GitHub

Детальная инструкция по настройке и деплою Tarot Web App в Telegram Mini App через GitHub Pages.

---

## 📋 Содержание

1. [Предварительные требования](#предварительные-требования)
2. [Шаг 1: Проверка деплоя на GitHub Pages](#шаг-1-проверка-деплоя-на-github-pages)
3. [Шаг 2: Настройка бота через BotFather](#шаг-2-настройка-бота-через-botfather)
4. [Шаг 3: Настройка Web App URL](#шаг-3-настройка-web-app-url)
5. [Шаг 4: Настройка Menu Button (опционально)](#шаг-4-настройка-menu-button-опционально)
6. [Шаг 5: Проверка работы](#шаг-5-проверка-работы)
7. [Шаг 6: Обновление приложения](#шаг-6-обновление-приложения)
8. [Решение проблем](#решение-проблем)
9. [Дополнительные настройки](#дополнительные-настройки)

---

## ✅ Предварительные требования

- ✅ GitHub репозиторий настроен и проект задеплоен на GitHub Pages
- ✅ Бот создан в Telegram через BotFather
- ✅ Токен бота: `7970012393:AAFQ98YFp4rMdMyaCSUOFMwbjBmdA1-kaIA`
- ✅ Приложение доступно по HTTPS (GitHub Pages автоматически предоставляет HTTPS)

---

## 🚀 Шаг 1: Проверка деплоя на GitHub Pages

### 1.1 Проверьте статус деплоя

1. Откройте репозиторий на GitHub: `https://github.com/mrdudekowski/tarot-web`
2. Перейдите в раздел **Settings** → **Pages**
3. Убедитесь, что:
   - **Source** установлен на **GitHub Actions**
   - Или на **Deploy from a branch** с веткой `gh-pages` или `main`
   - Статус показывает **✅ Your site is live at...**

### 1.2 Определите URL вашего приложения

URL вашего приложения будет в формате:
```
https://mrdudekowski.github.io/tarot-web/
```

**⚠️ ВАЖНО:** 
- URL должен заканчиваться на `/` (слэш)
- URL должен начинаться с `https://` (обязательно HTTPS)
- Убедитесь, что приложение открывается в браузере по этому адресу

### 1.3 Проверьте работу приложения

Откройте URL в браузере и убедитесь, что:
- ✅ Приложение загружается без ошибок
- ✅ Все изображения загружаются корректно
- ✅ Навигация работает
- ✅ Консоль браузера не показывает критических ошибок

---

## 🤖 Шаг 2: Настройка бота через BotFather

### 2.1 Откройте BotFather

1. Откройте Telegram
2. Найдите бота **@BotFather** в поиске
3. Начните диалог: `/start`

### 2.2 Выберите или найдите своего бота

Отправьте команду:
```
/mybots
```

Выберите вашего бота из списка. Если бота еще нет в списке, создайте нового:
```
/newbot
```

Следуйте инструкциям BotFather для создания бота.

---

## 🔗 Шаг 3: Настройка Web App URL

### 3.1 Откройте настройки Web App

В диалоге с BotFather:

1. Отправьте команду:
   ```
   /mybots
   ```

2. Выберите вашего бота из списка

3. Выберите пункт: **"Bot Settings"** или **"⚙️ Bot Settings"**

4. Выберите: **"Menu Button"** или **"Web App"**

### 3.2 Установите Web App URL

BotFather попросит ввести URL вашего Web App.

Введите:
```
https://mrdudekowski.github.io/tarot-web/
```

**⚠️ ВАЖНО:** 
- URL должен быть полным (включая `https://`)
- URL должен заканчиваться на `/`
- Убедитесь, что нет лишних пробелов

### 3.3 Подтверждение

BotFather подтвердит установку URL:
```
✅ Web App URL has been set successfully!
```

---

## 🎛️ Шаг 4: Настройка Menu Button (опционально)

Menu Button позволяет пользователям открывать ваше приложение прямо из меню бота.

### 4.1 Настройка кнопки меню

1. В диалоге с BotFather отправьте:
   ```
   /mybots
   ```

2. Выберите вашего бота

3. Выберите: **"Bot Settings"** → **"Menu Button"**

4. Выберите: **"Configure menu button"**

5. Введите текст для кнопки (например):
   ```
   🔮 Открыть Таро
   ```
   или
   ```
   Таро-расклады
   ```

6. Введите URL (тот же, что и для Web App):
   ```
   https://mrdudekowski.github.io/tarot-web/
   ```

7. BotFather подтвердит:
   ```
   ✅ Menu button has been configured!
   ```

### 4.2 Как это будет выглядеть

После настройки пользователи увидят кнопку меню в интерфейсе бота:
- В мобильном приложении: кнопка в правом нижнем углу рядом с полем ввода
- В десктопе: кнопка в меню бота

---

## ✅ Шаг 5: Проверка работы

### 5.1 Откройте бота в Telegram

1. Найдите вашего бота в Telegram
2. Начните диалог с ботом: `/start`
3. Если настроен Menu Button:
   - Нажмите на кнопку меню (🔮 Открыть Таро)
   - Приложение должно открыться внутри Telegram

### 5.2 Проверка функциональности

Убедитесь, что работает:
- ✅ Приложение загружается внутри Telegram
- ✅ Интерфейс отображается корректно
- ✅ Навигация работает
- ✅ Все изображения карт загружаются
- ✅ Расклады генерируются корректно
- ✅ Haptic feedback работает (вибрация при взаимодействии)

### 5.3 Проверка в разных клиентах

Протестируйте в:
- 📱 **Мобильном приложении Telegram** (iOS/Android)
- 💻 **Десктопном приложении Telegram** (Windows/Mac/Linux)
- 🌐 **Веб-версии Telegram** (web.telegram.org)

---

## 🔄 Шаг 6: Обновление приложения

### 6.1 Автоматическое обновление

Приложение обновляется автоматически при каждом пуше в ветку `main`:

1. Внесите изменения в код
2. Запушьте в `main`:
   ```bash
   git add .
   git commit -m "Update app"
   git push origin main
   ```

3. GitHub Actions автоматически:
   - Соберет приложение
   - Задеплоит на GitHub Pages
   - Обычно это занимает 1-3 минуты

4. Проверьте статус деплоя:
   - GitHub → Actions → последний workflow

### 6.2 Обновление URL (если изменился репозиторий)

Если вы переименовали репозиторий или изменился URL:

1. Обновите URL в BotFather:
   ```
   /mybots → выберите бота → Bot Settings → Menu Button
   ```

2. Введите новый URL

3. Обновите `.github/workflows/deploy.yml` если нужно изменить `BASE_URL`

---

## 🔧 Решение проблем

### Проблема 1: Приложение не открывается в Telegram

**Симптомы:**
- При нажатии на кнопку меню ничего не происходит
- Отображается ошибка "Failed to load"

**Решения:**

1. **Проверьте URL:**
   - URL должен быть доступен по HTTPS
   - Откройте URL в браузере и убедитесь, что приложение работает
   - Убедитесь, что URL заканчивается на `/`

2. **Проверьте настройки бота:**
   ```
   /mybots → ваш бот → Bot Settings → Menu Button
   ```
   Убедитесь, что URL указан правильно

3. **Очистите кеш Telegram:**
   - Перезапустите приложение Telegram
   - Удалите и заново добавьте бота

### Проблема 2: Изображения не загружаются

**Симптомы:**
- Карты отображаются без изображений
- В консоли ошибки 404 для изображений

**Решения:**

1. **Проверьте пути к изображениям:**
   - Убедитесь, что все пути используют `import.meta.env.BASE_URL`
   - Проверьте, что в workflow установлен правильный `BASE_URL`

2. **Проверьте сборку:**
   - Убедитесь, что файлы из `public/` копируются в `dist/`
   - Проверьте сборку локально: `npm run build` в папке `frontend/`

### Проблема 3: Приложение открывается, но не работает корректно

**Симптомы:**
- Интерфейс отображается, но функции не работают
- Ошибки в консоли

**Решения:**

1. **Проверьте консоль браузера:**
   - В Telegram Desktop: нажмите `Ctrl+Shift+I` (Windows) или `Cmd+Option+I` (Mac)
   - Посмотрите на ошибки в консоли

2. **Проверьте работу Telegram Web App SDK:**
   - Убедитесь, что `@twa-dev/sdk` установлен: `npm list @twa-dev/sdk`
   - Проверьте инициализацию в `main.js`

3. **Проверьте CORS (если используете API):**
   - GitHub Pages не имеет ограничений CORS для статических файлов
   - Если есть внешний API, убедитесь, что он разрешает запросы с вашего домена

### Проблема 4: Изменения не отображаются после деплоя

**Симптомы:**
- Запушены изменения, но приложение не обновилось

**Решения:**

1. **Проверьте статус деплоя:**
   - GitHub → Actions → проверьте последний workflow
   - Убедитесь, что workflow завершился успешно

2. **Очистите кеш:**
   - Очистите кеш браузера в Telegram
   - Перезапустите приложение Telegram

3. **Проверьте кеш GitHub Pages:**
   - GitHub Pages может кешировать старую версию
   - Подождите 5-10 минут и попробуйте снова

---

## 🎨 Дополнительные настройки

### Настройка темы приложения

Telegram Mini App может использовать цветовую схему Telegram:

```javascript
// В main.js или отдельном файле
import { WebApp } from '@twa-dev/sdk'

if (WebApp) {
  // Использовать цветовую схему Telegram
  WebApp.setHeaderColor('#6B46C1') // Фиолетовый
  WebApp.setBackgroundColor('#0a0a1a') // Темный фон
  
  // Развернуть на весь экран
  WebApp.expand()
  
  // Скрыть кнопку закрытия (опционально)
  // WebApp.disableClosingConfirmation()
}
```

### Настройка Back Button

Добавьте обработку кнопки "Назад" в Telegram:

```javascript
import { WebApp } from '@twa-dev/sdk'

if (WebApp) {
  // Показать кнопку "Назад"
  WebApp.BackButton.show()
  
  // Обработчик нажатия
  WebApp.BackButton.onClick(() => {
    // Логика возврата (например, router.go(-1))
    router.back()
  })
  
  // Скрыть кнопку на главной странице
  if (route.path === '/') {
    WebApp.BackButton.hide()
  }
}
```

### Настройка Main Button

Добавьте кнопку действий внизу экрана:

```javascript
import { WebApp } from '@twa-dev/sdk'

if (WebApp) {
  WebApp.MainButton.setText('Создать расклад')
  WebApp.MainButton.show()
  
  WebApp.MainButton.onClick(() => {
    // Действие при нажатии
    router.push('/reading')
  })
}
```

### Настройка Haptic Feedback

Тактильная обратная связь для лучшего UX:

```javascript
if (window.Telegram?.WebApp?.HapticFeedback) {
  // Легкая вибрация
  window.Telegram.WebApp.HapticFeedback.impactOccurred('light')
  
  // Средняя вибрация
  window.Telegram.WebApp.HapticFeedback.impactOccurred('medium')
  
  // Сильная вибрация
  window.Telegram.WebApp.HapticFeedback.impactOccurred('heavy')
  
  // Вибрация успеха
  window.Telegram.WebApp.HapticFeedback.notificationOccurred('success')
  
  // Вибрация ошибки
  window.Telegram.WebApp.HapticFeedback.notificationOccurred('error')
}
```

### Получение данных пользователя

```javascript
import { WebApp } from '@twa-dev/sdk'

if (WebApp) {
  const user = WebApp.initDataUnsafe?.user
  
  if (user) {
    console.log('ID пользователя:', user.id)
    console.log('Имя:', user.first_name)
    console.log('Фамилия:', user.last_name)
    console.log('Username:', user.username)
    console.log('Язык:', user.language_code)
  }
}
```

---

## 📊 Проверочный чеклист

Используйте этот чеклист для проверки готовности:

- [ ] Репозиторий задеплоен на GitHub Pages
- [ ] Приложение доступно по HTTPS URL
- [ ] Все изображения загружаются корректно
- [ ] Бот создан и активирован
- [ ] Web App URL настроен в BotFather
- [ ] Menu Button настроен (опционально)
- [ ] Приложение открывается в Telegram
- [ ] Все функции работают корректно
- [ ] Протестировано на мобильном и десктопе

---

## 🔐 Безопасность

### Важные рекомендации:

1. **Не публикуйте токен бота:**
   - Токен уже был указан в запросе (для примера)
   - В реальности храните токен в секретах GitHub Actions или переменных окружения
   - Если токен скомпрометирован, немедленно создайте новый через BotFather

2. **Валидация initData:**
   - Если используете данные пользователя, всегда валидируйте `initData`
   - Не доверяйте данным без проверки подписи

3. **HTTPS обязателен:**
   - Telegram требует HTTPS для Web Apps
   - GitHub Pages автоматически предоставляет HTTPS

---

## 📚 Полезные ссылки

- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [Telegram Web Apps Documentation](https://core.telegram.org/bots/webapps)
- [@twa-dev/sdk на npm](https://www.npmjs.com/package/@twa-dev/sdk)
- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [BotFather в Telegram](https://t.me/BotFather)

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте раздел "Решение проблем" выше
2. Проверьте логи GitHub Actions
3. Проверьте консоль браузера в Telegram Desktop
4. Убедитесь, что все шаги выполнены корректно

---

**Удачного деплоя! 🔮✨**
