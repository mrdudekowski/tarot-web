# 🎯 ФУНДАМЕНТАЛЬНЫЕ ПРИНЦИПЫ РАЗРАБОТКИ

> **Цель этого документа:** Сформировать рациональный, преднамеренный и эффективный подход к разработке, который помогает создавать качественный, поддерживаемый код с минимальным техническим долгом.

---

## 🏛️ ФУНДАМЕНТАЛЬНЫЕ ПРИНЦИПЫ (Приоритет #1)

Эти принципы - **основа всего**. Их нарушение ведёт к хрупкому коду, багам и росту технического долга.

### 1. Type Safety First - Типобезопасность превыше всего
**Фундаментальность:** Это не просто "хорошая практика" - это **защита от целого класса ошибок** на этапе компиляции, а не в runtime.

**Почему критично:**
- Предотвращает 80%+ runtime ошибок, связанных с типами
- Делает код самодокументируемым (типы = документация)
- Позволяет безопасно рефакторить
- IDE может предоставлять точные подсказки и автодополнение

**Что происходит при игнорировании:**
- Скрытые баги, которые проявляются только в production
- Невозможность безопасного рефакторинга
- Необходимость в большом количестве runtime проверок
- Сложность понимания кода новыми разработчиками

**Практика:**
```typescript
// ✅ Хорошо - строгая типизация
interface User {
  id: string;
  name: string;
  email: string;
}

function getUserById(id: string): Promise<User> {
  // TypeScript гарантирует, что вернётся User
}

// ❌ Плохо - отсутствие типов
function getUserById(id: any): Promise<any> {
  // Что вернётся? Что ожидается? Неизвестно до runtime
}
```

**Правило:** TypeScript strict mode **всегда**. `any` только в крайних случаях с явным комментарием "почему".

---

### 2. Immutability - Неизменяемость данных
**Фундаментальность:** Мутации - источник непредсказуемого поведения, особенно в React. Неизменяемость делает код предсказуемым и отлаживаемым.

**Почему критично:**
- React полагается на сравнение ссылок для определения изменений
- Предсказуемость: функция с теми же входными данными всегда даёт тот же результат
- Легче отлаживать: можно логировать состояние без страха его изменения
- Безопасность в многопоточности (если понадобится)

**Что происходит при игнорировании:**
- Баги с обновлением UI в React (компонент не перерисовывается)
- Непредсказуемое поведение при отладке
- Сложность отслеживания изменений состояния
- Невозможность использования time-travel debugging

**Практика:**
```typescript
// ✅ Хорошо - создаём новые объекты
function addItem(cart: CartItem[], newItem: CartItem): CartItem[] {
  return [...cart, newItem]; // новый массив
}

const updatedUser = { ...user, name: 'New Name' }; // новый объект

// ❌ Плохо - мутация
function addItem(cart: CartItem[], newItem: CartItem) {
  cart.push(newItem); // мутация исходного массива!
  return cart;
}

user.name = 'New Name'; // мутация исходного объекта!
```

**Правило:** **Никогда** не мутируй входящие параметры, props, state. Всегда создавай новые структуры данных.

---

### 3. Explicit Dependencies - Явные зависимости
**Фундаментальность:** Скрытые зависимости делают код непредсказуемым, не тестируемым и сложным для понимания.

**Почему критично:**
- Тестируемость: можно подменить зависимости моками
- Предсказуемость: видно от чего зависит функция
- Отслеживание: можно найти все места использования
- Рефакторинг: безопасно менять зависимости

**Что происходит при игнорировании:**
- Невозможность тестирования в изоляции
- Скрытые побочные эффекты
- Сложность понимания потока данных
- Неожиданные баги при изменении глобального состояния

**Практика:**
```typescript
// ✅ Хорошо - явные зависимости
import { apiClient } from './services/api';

function fetchUserData(userId: string, client = apiClient) {
  return client.get(`/users/${userId}`);
}

// Тестирование
const mockClient = { get: jest.fn() };
fetchUserData('123', mockClient);

// ❌ Плохо - скрытые зависимости
function fetchUserData(userId: string) {
  // Откуда берётся fetch? API_URL? Непонятно
  return fetch(`${API_URL}/users/${userId}`);
}
```

**Правило:** Все зависимости должны быть **явными** - через параметры, импорты, или dependency injection.

---

### 4. Single Responsibility Principle - Одна ответственность
**Фундаментальность:** Функция/компонент с одной задачей легче понимать, тестировать, переиспользовать и изменять.

**Почему критично:**
- Тестируемость: одна вещь = один тест
- Переиспользование: можно использовать в разных контекстах
- Поддержка: изменения изолированы
- Понимание: меньше когнитивной нагрузки

**Что происходит при игнорировании:**
- Сложность тестирования (нужно мокать много вещей)
- Невозможность переиспользования
- Каскадные изменения при модификации
- Высокая когнитивная нагрузка при чтении

**Практика:**
```typescript
// ✅ Хорошо - одна ответственность
function calculateTotalPrice(items: CartItem[]): number {
  return items.reduce((sum, item) => sum + item.price * item.quantity, 0);
}

function applyDiscount(total: number, discount: number): number {
  return total * (1 - discount);
}

// ❌ Плохо - много ответственностей
function processOrder(items: any[], discount: any, tax: any, shipping: any) {
  // 50+ строк, делает всё: валидацию, расчёты, форматирование, логирование
}
```

**Правило:** Функция должна делать **одну вещь** и делать её хорошо. Максимум 20 строк кода на функцию.

---

### 5. DRY - Don't Repeat Yourself
**Фундаментальность:** Дублирование кода = дублирование багов. Одно изменение должно быть в одном месте.

**Почему критично:**
- Меньше кода = меньше багов
- Единая точка изменений
- Консистентность поведения
- Снижение технического долга

**Что происходит при игнорировании:**
- Баги в нескольких местах при одном изменении
- Несогласованное поведение
- Сложность рефакторинга
- Рост технического долга

**Практика:**
```typescript
// ✅ Хорошо - переиспользование
const API_ENDPOINTS = {
  USERS: '/api/users',
  CARDS: '/api/cards',
} as const;

function createApiCall(endpoint: string) {
  return async (params?: Record<string, unknown>) => {
    return fetch(`${API_BASE}${endpoint}`, { /* ... */ });
  };
}

// ❌ Плохо - дублирование
function fetchUsers() {
  return fetch('http://localhost:3000/api/users');
}
function fetchCards() {
  return fetch('http://localhost:3000/api/cards');
}
```

**Правило:** Если код повторяется **2+ раза**, вынеси его в функцию/компонент/утилиту.

---

### 6. Fail Fast - Падай быстро
**Фундаментальность:** Ошибки должны обнаруживаться как можно раньше - на этапе компиляции, а не в production.

**Почему критично:**
- Раннее обнаружение дешевле (компиляция vs production баг)
- Безопасность: некорректные данные не попадают в систему
- Отладка: ошибка близко к источнику проблемы
- Пользовательский опыт: лучше упасть сразу, чем работать с неверными данными

**Что происходит при игнорировании:**
- Баги в production
- Некорректные данные в базе
- Сложность отладки (ошибка далеко от источника)
- Плохой UX (приложение работает, но неправильно)

**Практика:**
```typescript
// ✅ Хорошо - валидация на входе
function divide(a: number, b: number): number {
  if (b === 0) {
    throw new Error('Division by zero is not allowed');
  }
  return a / b;
}

// Валидация API данных
function parseUser(data: unknown): User {
  const schema = z.object({
    id: z.string(),
    name: z.string(),
    email: z.string().email(),
  });
  return schema.parse(data); // упадёт сразу, если данные неверны
}

// ❌ Плохо - молчаливые ошибки
function divide(a: any, b: any): any {
  return a / b; // Может вернуть NaN или Infinity - скрытая ошибка
}
```

**Правило:** Валидируй данные на **границах** (API, формы, функции). Ошибки должны быть **явными**.

---

### 7. Composition over Inheritance - Композиция вместо наследования
**Фундаментальность:** Композиция даёт гибкость и переиспользуемость без хрупкой иерархии классов.

**Почему критично:**
- Гибкость: можно комбинировать функциональность
- Переиспользование: компоненты независимы
- Тестируемость: можно тестировать части отдельно
- Избежание проблем "diamond inheritance"

**Что происходит при игнорировании:**
- Хрупкая иерархия классов
- Сложность изменения базового класса
- Невозможность переиспользования в других контекстах
- Тесная связанность компонентов

**Практика:**
```typescript
// ✅ Хорошо - композиция
function useAuth() {
  const [user, setUser] = useState<User | null>(null);
  return { user, login, logout };
}

function UserProfile() {
  const { user, logout } = useAuth(); // композиция
  // можно использовать useAuth в любом компоненте
}

// ❌ Плохо - наследование
class UserComponent extends AuthComponent {
  // жёсткая связь, сложно переиспользовать
}
```

**Правило:** Предпочитай **композицию** (hooks, компоненты, функции) наследованию.

---

## 🎯 ПРИНЦИПЫ РАЦИОНАЛЬНОГО ПОДХОДА

### 8. MVP First, Optimize Later
**Принцип:** Сначала сделай рабочую версию, затем оптимизируй. Избегай преждевременной оптимизации.

**Почему:**
- Рабочий код > идеальный код, который не работает
- Оптимизация без измерения = угадывание
- Простое решение часто достаточно
- Можно итерировать и улучшать

**Практика:**
```typescript
// ✅ Хорошо - сначала простое решение
function filterItems(items: Item[], query: string) {
  return items.filter(item => 
    item.name.toLowerCase().includes(query.toLowerCase())
  );
}

// Позже, если нужно оптимизировать (только после измерения!)
function filterItemsOptimized(items: Item[], query: string) {
  const lowerQuery = query.toLowerCase();
  // оптимизация только если доказана необходимость
}

// ❌ Плохо - преждевременная оптимизация
function filterItems(items: Item[], query: string) {
  // Сложная оптимизация с индексами, кэшированием и т.д.
  // когда простая версия была бы достаточна
}
```

**Правило:** Делай **самое простое решение**, которое работает. Оптимизируй только после измерения и доказательства необходимости.

---

### 9. Self-Documenting Code - Самодокументируемый код
**Принцип:** Код должен быть понятен без комментариев. Комментарии только для "почему", не "что".

**Почему:**
- Код читается в 10 раз чаще, чем пишется
- Комментарии устаревают, код - источник истины
- Понятные имена снижают когнитивную нагрузку
- Меньше вопросов "что это делает?"

**Практика:**
```typescript
// ✅ Хорошо - самодокументируемый код
const isUserAuthenticated = checkAuthStatus(user);
const hasValidSubscription = 
  subscription?.isActive && subscription?.expiresAt > Date.now();

if (isUserAuthenticated && hasValidSubscription) {
  grantAccess();
}

// Комментарий только для "почему"
// Используем setTimeout вместо requestAnimationFrame, 
// потому что нужно гарантировать выполнение после всех обновлений DOM
setTimeout(() => updateLayout(), 0);

// ❌ Плохо - непонятные имена
const auth = check(user); // что проверяется?
const sub = sub?.a && sub?.e > Date.now(); // что такое a и e?
if (auth && sub) { // что проверяется?
  grant();
}
```

**Правило:** Имена должны отвечать на вопросы: **Что это?** (переменная), **Что делает?** (функция), **Почему?** (комментарий).

---

### 10. Understand Before Optimize - Понять перед оптимизацией
**Принцип:** Не оптимизируй то, что не измерено. Не оптимизируй то, что не является узким местом.

**Почему:**
- Преждевременная оптимизация - корень всех зол
- Оптимизация усложняет код
- 80% времени тратится на 20% кода
- Измерение показывает реальные проблемы

**Практика:**
```typescript
// ✅ Хорошо - измеряем, затем оптимизируем
function processData(data: Data[]) {
  // Простая версия
  return data.map(transform).filter(isValid);
}

// После профилирования обнаружили узкое место
function processDataOptimized(data: Data[]) {
  // Оптимизация только узкого места
  const result: Data[] = [];
  for (const item of data) {
    const transformed = transform(item);
    if (isValid(transformed)) {
      result.push(transformed);
    }
  }
  return result;
}

// ❌ Плохо - оптимизация без измерения
function processData(data: Data[]) {
  // Сложная оптимизация "на всякий случай"
  // которая может быть не нужна
}
```

**Правило:** Оптимизируй только после **измерения** и доказательства, что это узкое место.

---

### 11. Explicit Over Implicit - Явность превыше неявности
**Принцип:** Код должен быть явным. Магические значения, скрытая логика, неочевидные зависимости - зло.

**Почему:**
- Явный код легче понимать
- Меньше сюрпризов
- Легче отлаживать
- Легче изменять

**Практика:**
```typescript
// ✅ Хорошо - явные константы
const MAX_RETRY_ATTEMPTS = 3;
const RETRY_DELAY_MS = 1000;

async function fetchWithRetry(url: string) {
  for (let i = 0; i < MAX_RETRY_ATTEMPTS; i++) {
    try {
      return await fetch(url);
    } catch (error) {
      if (i === MAX_RETRY_ATTEMPTS - 1) throw error;
      await delay(RETRY_DELAY_MS);
    }
  }
}

// ❌ Плохо - магические числа
async function fetchWithRetry(url: string) {
  for (let i = 0; i < 3; i++) { // почему 3?
    try {
      return await fetch(url);
    } catch (error) {
      if (i === 2) throw error; // почему 2?
      await delay(1000); // почему 1000?
    }
  }
}
```

**Правило:** Все магические значения должны быть **константами** с понятными именами.

---

### 12. Error Handling - Обработка ошибок
**Принцип:** Всегда обрабатывай ошибки явно. Не игнорируй их, не используй пустые catch блоки.

**Почему:**
- Пользователь должен понимать, что произошло
- Разработчик должен видеть проблемы
- Ошибки - это информация, не что-то, что нужно скрыть

**Практика:**
```typescript
// ✅ Хорошо - явная обработка ошибок
async function fetchUserData(userId: string): Promise<User> {
  try {
    const response = await api.get(`/users/${userId}`);
    return response.data;
  } catch (error) {
    if (error instanceof ApiError) {
      logger.error('Failed to fetch user', { userId, error });
      throw new UserNotFoundError(`User ${userId} not found`);
    }
    throw error;
  }
}

// ❌ Плохо - игнорирование ошибок
async function fetchUserData(userId: string) {
  try {
    return await api.get(`/users/${userId}`);
  } catch (error) {
    // молча игнорируем - пользователь не узнает об ошибке
  }
}
```

**Правило:** Ошибки должны быть **явными**, **типизированными** и **логируемыми**.

---

## 📊 ПРИНЦИПЫ ИТЕРАЦИЙ И РЕЗУЛЬТАТА

### 13. Working Code > Perfect Code
**Принцип:** Рабочий код важнее идеального. Сначала сделай, затем улучшай.

**Почему:**
- Идеальный код, который не работает, бесполезен
- Можно итерировать и улучшать
- Реальный feedback важнее теоретического совершенства
- Технический долг можно планомерно снижать

**Практика:**
```typescript
// ✅ Хорошо - сначала рабочая версия
function calculateTotal(items: Item[]) {
  let total = 0;
  for (const item of items) {
    total += item.price * item.quantity;
  }
  return total;
}

// Позже улучшаем (если нужно)
function calculateTotal(items: Item[]) {
  return items.reduce((sum, item) => sum + item.price * item.quantity, 0);
}

// ❌ Плохо - попытка сделать идеально с первой попытки
// Тратим время на "идеальное" решение, которое может быть не нужно
```

**Правило:** Сначала **рабочая версия**, затем **итеративное улучшение**.

---

### 14. Technical Debt Awareness - Осознание технического долга
**Принцип:** Любая строка кода - это технический долг. Снижай его где возможно.

**Почему:**
- Код нужно поддерживать
- Технический долг накапливается
- Чем больше долга, тем медленнее разработка
- Планомерное снижение долга важно

**Практика:**
```typescript
// ✅ Хорошо - снижение долга
// Используем переиспользуемые компоненты
function Button({ variant, children }: ButtonProps) {
  return <button className={cn('btn', `btn-${variant}`)}>{children}</button>;
}

// Используем типы вместо any
interface User {
  id: string;
  name: string;
}

// ❌ Плохо - увеличение долга
// Дублирование кода
function PrimaryButton({ children }: { children: React.ReactNode }) {
  return <button className="btn btn-primary">{children}</button>;
}
function SecondaryButton({ children }: { children: React.ReactNode }) {
  return <button className="btn btn-secondary">{children}</button>;
}

// any везде
function processUser(user: any) { /* ... */ }
```

**Правило:** Принимай решения, которые **снижают** технический долг, а не увеличивают его.

---

## 🎯 ЧЕК-ЛИСТ ПЕРЕД КОММИТОМ

- [ ] TypeScript strict mode, нет `any` без явной причины
- [ ] Нет мутаций данных (используются spread, map, filter)
- [ ] Все зависимости явные (параметры, импорты)
- [ ] Функции делают одну вещь (максимум 20 строк)
- [ ] Нет дублирования кода (DRY)
- [ ] Валидация на границах (API, формы)
- [ ] Ошибки обрабатываются явно
- [ ] Имена переменных и функций понятны
- [ ] Комментарии объясняют "почему", а не "что"
- [ ] Критическая логика покрыта тестами
- [ ] Рабочий код (не идеальный, но работающий)

---

## 📚 Дополнительные принципы

### KISS - Keep It Simple, Stupid
Делай самое простое решение, которое работает. Сложность добавляй только когда доказана необходимость.

### YAGNI - You Aren't Gonna Need It
Не добавляй функциональность, пока она не нужна. Будущие требования могут измениться.

### SOLID принципы
- **S**ingle Responsibility - одна ответственность
- **O**pen/Closed - открыт для расширения, закрыт для модификации
- **L**iskov Substitution - подтипы должны заменять базовые типы
- **I**nterface Segregation - много маленьких интерфейсов лучше одного большого
- **D**ependency Inversion - зависеть от абстракций, а не от конкретики

---

## 🎯 ПРИОРИТЕТЫ ПРИ ПРИНЯТИИ РЕШЕНИЙ

1. **Работоспособность** - код должен работать
2. **Правильность** - код должен делать то, что нужно
3. **Понятность** - код должен быть понятен
4. **Производительность** - код должен быть достаточно быстрым
5. **Идеальность** - код может быть идеальным (но не обязательно)

**Помни:** Идеальный код, который не работает, бесполезен. Рабочий код можно улучшить.
