# 🎨 TAILWIND CSS - BEST PRACTICES И ПРИНЦИПЫ

> **Важно:** В этом проекте Tailwind CSS - **основной инструмент** для стилизации. Кастомный CSS используется только когда Tailwind не может решить задачу.

---

## 🏛️ ФУНДАМЕНТАЛЬНЫЕ ПРИНЦИПЫ TAILWIND

### 1. Utility-First - основной подход (всегда)
**Принцип:** Используй утилитарные классы Tailwind вместо написания кастомного CSS. Это **правило по умолчанию**.

**Почему:**
- Быстрая разработка (не нужно переключаться между файлами)
- Консистентность дизайна (используются design tokens)
- Меньший размер бандла (PurgeCSS удаляет неиспользуемые стили)
- Нет конфликтов специфичности
- Легче поддерживать (стили рядом с компонентом)

**Когда можно отступить:**
- Сложные анимации, которые невозможно сделать через Tailwind
- Специфичные CSS возможности (container queries, :has() и т.д.)
- Третьесторонние библиотеки, требующие кастомных стилей

**Практика:**
```tsx
// ✅ Хорошо - используем утилиты Tailwind
<div className="flex items-center justify-between p-4 bg-white rounded-lg shadow-md">
  <h2 className="text-xl font-bold text-gray-800">Заголовок</h2>
  <button className="px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600">
    Кнопка
  </button>
</div>

// ❌ Плохо - кастомный CSS (когда можно использовать Tailwind)
<div className="custom-container">
  <h2 className="custom-title">Заголовок</h2>
  <button className="custom-button">Кнопка</button>
</div>
```

**Правило:** Tailwind утилиты - **по умолчанию**. Кастомный CSS только когда Tailwind не может решить задачу.

---

### 2. Design Tokens из конфига (всегда)
**Принцип:** Все цвета, размеры, отступы должны браться из `tailwind.config.js`. Никаких хардкод значений.

**Почему:**
- Единый дизайн-язык (консистентность)
- Легко менять тему (одно место изменений)
- Предсказуемость (знаешь какие значения доступны)
- Следует принципу Single Source of Truth

**Практика:**
```javascript
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#f0f9ff',
          500: '#3b82f6',
          900: '#1e3a8a',
        },
        tarot: {
          gold: '#d4af37',
          purple: '#6b46c1',
        },
      },
      spacing: {
        '18': '4.5rem',
        '88': '22rem',
      },
    },
  },
}
```

```tsx
// ✅ Хорошо - используем токены
<div className="bg-tarot-purple text-tarot-gold p-4">
  <div className="w-88 h-18">Контент</div>
</div>

// ❌ Плохо - хардкод значений
<div className="bg-[#6b46c1] text-[#d4af37] p-[1rem]">
  <div className="w-[22rem] h-[4.5rem]">Контент</div>
</div>
```

**Правило:** Все значения должны быть в конфиге. Произвольные значения `[value]` только когда нет подходящего токена.

---

### 3. @apply - только для повторяющихся паттернов
**Принцип:** Используй `@apply` **только** для часто повторяющихся комбинаций классов (3+ раза). В остальных случаях используй утилиты напрямую.

**Почему:**
- `@apply` уменьшает гибкость (сложнее переопределить)
- Утилиты более явные (видно что применяется)
- `@apply` может увеличить размер бандла

**Когда использовать @apply:**
- Повторяющиеся паттерны (кнопки, карточки)
- Комбинации классов, которые всегда используются вместе

**Практика:**
```css
/* ✅ Хорошо - повторяющийся паттерн (кнопка используется везде) */
@layer components {
  .btn-primary {
    @apply px-4 py-2 bg-blue-500 text-white rounded-lg font-semibold;
    @apply hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500;
    @apply transition-colors duration-200;
  }
  
  .card {
    @apply bg-white rounded-lg shadow-md p-6;
  }
}

/* ❌ Плохо - @apply для одноразового использования */
@layer components {
  .container {
    @apply flex items-center justify-between p-4;
    /* Лучше оставить как утилиты - более гибко */
  }
}
```

**Правило:** `@apply` только для паттернов, которые повторяются **3+ раза**. В остальных случаях - утилиты.

---

### 4. Mobile First - всегда
**Принцип:** Пиши стили для мобильных устройств, затем расширяй для больших экранов через responsive префиксы.

**Почему:**
- Большинство пользователей на мобильных
- Меньший CSS для мобильных (быстрее загрузка)
- Прогрессивное улучшение (базовая версия работает везде)

**Практика:**
```tsx
// ✅ Хорошо - mobile first
<div className="
  flex flex-col gap-2 p-4
  md:flex-row md:gap-4 md:p-6
  lg:gap-6 lg:p-8
">
  <div className="w-full md:w-1/2 lg:w-1/3">Контент</div>
</div>

// ❌ Плохо - desktop first
<div className="
  flex-row gap-6 p-8
  max-md:flex-col max-md:gap-2 max-md:p-4
">
  <div className="w-1/3 max-md:w-full">Контент</div>
</div>
```

**Правило:** Всегда начинай с мобильных стилей, затем добавляй `md:`, `lg:`, `xl:` префиксы.

---

### 5. Группировка и порядок классов
**Принцип:** Группируй классы логически для читаемости. Используй Prettier plugin для автоматической сортировки.

**Порядок (рекомендуемый):**
1. Layout (flex, grid, display)
2. Spacing (padding, margin, gap)
3. Sizing (width, height)
4. Typography (font, text, line-height)
5. Colors (background, text, border)
6. Effects (shadow, opacity, transform)
7. Interactions (hover, focus, active)
8. Responsive (md:, lg:)

**Практика:**
```tsx
// ✅ Хорошо - логическая группировка
<button className="
  /* Layout */
  flex items-center justify-center
  /* Spacing */
  px-4 py-2 gap-2
  /* Typography */
  text-base font-semibold
  /* Colors */
  bg-blue-500 text-white
  /* Effects */
  rounded-lg shadow-md
  /* Interactions */
  hover:bg-blue-600
  transition-colors
">
  Кнопка
</button>

// ❌ Плохо - хаотичный порядок
<button className="bg-blue-500 text-base px-4 flex rounded-lg py-2 items-center shadow-md">
  Кнопка
</button>
```

**Правило:** Группируй классы логически или используй Prettier plugin для автоматической сортировки.

---

### 6. Компоненты для сложных паттернов
**Принцип:** Сложные повторяющиеся UI-паттерны выноси в React компоненты, а не в CSS классы через `@apply`.

**Почему:**
- Переиспользование с типизацией
- Логика вместе со стилями
- Легче тестировать
- Следует принципу Composition

**Практика:**
```tsx
// ✅ Хорошо - компонент с Tailwind классами
interface CardProps {
  title?: string;
  children: React.ReactNode;
  className?: string;
}

function Card({ title, children, className = '' }: CardProps) {
  return (
    <div className={cn(
      "bg-white rounded-lg shadow-md p-6",
      className
    )}>
      {title && (
        <h3 className="text-xl font-bold text-gray-800 mb-4">{title}</h3>
      )}
      {children}
    </div>
  );
}

// ❌ Плохо - кастомный CSS класс
<div className="custom-card">
  <h3 className="custom-card-title">Заголовок</h3>
  <div className="custom-card-content">Контент</div>
</div>
```

**Правило:** Сложные паттерны (с логикой, условиями) - React компоненты. Простые повторяющиеся стили - `@apply`.

---

### 7. Условные классы через `cn()` утилиту
**Принцип:** Используй библиотеку `clsx` или `cn` (из class-variance-authority) для условных классов. Никогда не используй шаблонные строки.

**Почему:**
- Чистый код
- Типобезопасность (с class-variance-authority)
- Избегание ошибок (нет проблем с пробелами)
- Следует принципу Explicit Dependencies

**Практика:**
```tsx
import { cn } from '@/lib/utils';

// ✅ Хорошо
<button className={cn(
  "px-4 py-2 rounded-lg",
  isActive && "bg-blue-500 text-white",
  isDisabled && "opacity-50 cursor-not-allowed",
  className
)}>
  Кнопка
</button>

// ❌ Плохо - шаблонные строки
<button className={`
  px-4 py-2 rounded-lg
  ${isActive ? 'bg-blue-500 text-white' : ''}
  ${isDisabled ? 'opacity-50 cursor-not-allowed' : ''}
`}>
  Кнопка
</button>
```

**Правило:** Всегда используй `cn()` для условных классов. Никогда шаблонные строки.

---

### 8. Dark Mode через класс
**Принцип:** Используй `dark:` префикс для темной темы. Переключай через класс на корневом элементе.

**Практика:**
```tsx
// ✅ Хорошо
<div className="
  bg-white text-gray-900
  dark:bg-gray-900 dark:text-white
">
  <h1 className="text-2xl font-bold text-gray-800 dark:text-gray-100">
    Заголовок
  </h1>
</div>

// tailwind.config.js
module.exports = {
  darkMode: 'class', // или 'media' для системной темы
}
```

**Правило:** Используй `dark:` префикс для всех элементов, которые должны меняться в темной теме.

---

### 9. Избегай произвольных значений
**Принцип:** Используй произвольные значения `[value]` только когда нет подходящего токена в конфиге.

**Почему:**
- Консистентность дизайна
- Легче поддерживать (значения в одном месте)
- Лучше производительность (меньше уникальных классов)

**Практика:**
```tsx
// ✅ Хорошо - используем токены
<div className="w-64 h-32 p-4 text-lg">Контент</div>

// ⚠️ Допустимо - когда нет подходящего токена
<div className="w-[13.5rem] h-[200px]">Специфичный размер</div>
// Но лучше добавить в конфиг:
// spacing: { '54': '13.5rem' }

// ❌ Плохо - когда есть токен
<div className="w-[16rem] h-[8rem]">Контент</div>
// Лучше: w-64 h-32
```

**Правило:** Произвольные значения только когда нет токена. Если значение используется 2+ раза, добавь в конфиг.

---

### 10. Кастомизация через extend, не override
**Принцип:** Используй `theme.extend` вместо `theme` для добавления токенов. Сохраняй дефолтные значения Tailwind.

**Почему:**
- Сохраняешь все дефолтные значения Tailwind
- Можешь использовать как свои, так и дефолтные токены
- Легче обновлять Tailwind (не теряешь дефолты)

**Практика:**
```javascript
// ✅ Хорошо - extend
module.exports = {
  theme: {
    extend: {
      colors: {
        brand: '#3b82f6',
        // Дефолтные цвета Tailwind (blue, red, etc.) всё ещё доступны
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
        // Дефолтные шрифты всё ещё доступны
      },
    },
  },
}

// ❌ Плохо - override (теряем дефолты)
module.exports = {
  theme: {
    colors: {
      brand: '#3b82f6',
      // Потеряли все дефолтные цвета Tailwind!
    },
  },
}
```

**Правило:** Всегда используй `extend`, никогда не переопределяй `theme` напрямую.

---

### 11. Производительность - правильный content
**Принцип:** Настрой правильный `content` в конфиге для удаления неиспользуемых стилей (PurgeCSS).

**Практика:**
```javascript
// tailwind.config.js
module.exports = {
  content: [
    './index.html',
    './src/**/*.{js,ts,jsx,tsx}',
    // Включай все файлы, где используются классы Tailwind
    // Включай даже файлы с динамическими классами
  ],
  // ...
}
```

**Правило:** `content` должен включать **все** файлы, где могут быть классы Tailwind (включая динамические).

---

### 12. Анимации и transitions
**Принцип:** Используй встроенные утилиты Tailwind для анимаций. Кастомные анимации только для специфичных случаев.

**Практика:**
```tsx
// ✅ Хорошо - встроенные утилиты
<div className="
  transition-all duration-300 ease-in-out
  hover:scale-105 hover:shadow-lg
  animate-pulse
">
  Контент
</div>

// Кастомная анимация в конфиге
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      animation: {
        'card-flip': 'cardFlip 0.6s ease-in-out',
      },
      keyframes: {
        cardFlip: {
          '0%': { transform: 'rotateY(0deg)' },
          '100%': { transform: 'rotateY(180deg)' },
        },
      },
    },
  },
}
```

**Правило:** Используй встроенные анимации Tailwind. Кастомные только когда нужно что-то специфичное.

---

### 13. Accessibility - доступность
**Принцип:** Всегда учитывай доступность при использовании Tailwind классов.

**Практика:**
```tsx
// ✅ Хорошо - доступность
<button className="
  px-4 py-2 bg-blue-500 text-white rounded-lg
  focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2
  aria-label="Закрыть модальное окно"
">
  ×
</button>

// Используй screen-reader-only классы
<span className="sr-only">Скрытый текст для скринридеров</span>
```

**Правило:** Всегда добавляй focus states и aria-атрибуты для интерактивных элементов.

---

### 14. Организация кастомного CSS через @layer
**Принцип:** Если нужен кастомный CSS, используй `@layer` для правильной организации и приоритетов.

**Практика:**
```css
/* ✅ Хорошо - правильная организация */
@layer base {
  /* Сброс стилей, базовые стили */
  h1 {
    @apply text-3xl font-bold;
  }
}

@layer components {
  /* Компоненты */
  .btn-primary {
    @apply px-4 py-2 bg-blue-500;
  }
}

@layer utilities {
  /* Кастомные утилиты */
  .text-balance {
    text-wrap: balance;
  }
}
```

**Правило:** Весь кастомный CSS должен быть в `@layer` для правильного порядка применения.

---

## 🎯 ЧЕК-ЛИСТ ИСПОЛЬЗОВАНИЯ TAILWIND

- [ ] Использую утилитарные классы вместо кастомного CSS (по умолчанию)
- [ ] Все цвета и размеры из конфига (design tokens)
- [ ] Mobile-first подход для responsive
- [ ] Классы сгруппированы логически (или используется Prettier plugin)
- [ ] Использую `cn()` для условных классов (никогда шаблонные строки)
- [ ] Сложные паттерны вынесены в React компоненты
- [ ] `@apply` только для повторяющихся паттернов (3+ раза)
- [ ] `content` в конфиге настроен правильно
- [ ] Использую `extend`, а не override в конфиге
- [ ] Учитываю accessibility (focus states, aria-labels)
- [ ] Кастомный CSS организован через `@layer`
- [ ] Произвольные значения только когда нет токена

---

## 📚 Полезные ресурсы

- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [Tailwind UI Components](https://tailwindui.com/)
- [Headless UI](https://headlessui.com/) - доступные компоненты для Tailwind
- [Heroicons](https://heroicons.com/) - иконки для Tailwind проектов

---

## ⚠️ КОГДА ИСПОЛЬЗОВАТЬ КАСТОМНЫЙ CSS

Кастомный CSS допустим только в следующих случаях:

1. **Сложные анимации**, которые невозможно сделать через Tailwind
2. **Специфичные CSS возможности** (container queries, :has(), и т.д.)
3. **Третьесторонние библиотеки**, требующие кастомных стилей
4. **Критическая производительность** (но только после измерения!)

Во всех остальных случаях используй Tailwind утилиты.
