# 🎨 CSS - ПРИНЦИПЫ И ПРАВИЛА

> **Важно:** В этом проекте Tailwind CSS - основной инструмент стилизации. Этот документ описывает принципы написания **кастомного CSS**, когда Tailwind не может решить задачу. См. `tailwind.md` для правил работы с Tailwind.

---

## 🏛️ ФУНДАМЕНТАЛЬНЫЕ ПРИНЦИПЫ CSS

### 1. Когда использовать кастомный CSS
**Принцип:** Кастомный CSS используется **только** когда Tailwind не может решить задачу.

**Когда допустимо:**
- Сложные анимации, которые невозможно сделать через Tailwind
- Специфичные CSS возможности (container queries, :has(), и т.д.)
- Третьесторонние библиотеки, требующие кастомных стилей
- Критическая производительность (но только после измерения!)

**Правило:** Сначала попробуй решить через Tailwind. Кастомный CSS - последний вариант.

---

### 2. BEM методология (если не Tailwind)
**Принцип:** Если пишешь кастомный CSS, используй BEM (Block-Element-Modifier) для именования классов.

**Почему:**
- Предсказуемость именования
- Избежание конфликтов специфичности
- Понятная структура
- Легко найти связанные стили

**Практика:**
```css
/* ✅ Хорошо - BEM */
.card { }                    /* Block */
.card__title { }             /* Element */
.card__title--large { }      /* Modifier */
.card--highlighted { }       /* Block modifier */

/* HTML */
<div class="card card--highlighted">
  <h2 class="card__title card__title--large">Заголовок</h2>
</div>

/* ❌ Плохо - неструктурированные имена */
.cardTitle { }
.card-title-large { }
.cardHighlighted { }
```

**Правило:** Если пишешь кастомный CSS, используй BEM для именования.

---

### 3. Специфичность - понимай и контролируй
**Принцип:** Понимай как работает специфичность. Избегай излишней вложенности. Низкая специфичность = легче переопределять.

**Почему:**
- Предсказуемость стилей
- Легче переопределять
- Меньше конфликтов
- Следует принципу Explicit Dependencies

**Правило специфичности:**
- Inline styles: 1000
- IDs: 100
- Classes, attributes: 10
- Elements: 1

**Практика:**
```css
/* ✅ Хорошо - низкая специфичность */
.button { }
.button--primary { }
.button--disabled { }

/* ❌ Плохо - высокая специфичность */
.header .nav .menu .item .button.primary { }
/* Сложно переопределить, хрупкий код */
```

**Правило:** Держи специфичность **низкой**. Один класс лучше, чем вложенность.

---

### 4. Box Model - border-box всегда
**Принцип:** Всегда используй `border-box` для box-sizing. Это делает размеры предсказуемыми.

**Почему:**
- Предсказуемость размеров
- Легче считать (padding и border включены в width/height)
- Стандарт современной разработки

**Практика:**
```css
/* ✅ Хорошо - border-box везде */
*,
*::before,
*::after {
  box-sizing: border-box;
}

.element {
  width: 100px;
  padding: 10px;
  border: 2px solid;
  /* Итоговая ширина = 100px (не 124px) */
}

/* ❌ Плохо - content-box (дефолт) */
.element {
  width: 100px;
  padding: 10px;
  border: 2px solid;
  /* Итоговая ширина = 124px (неожиданно!) */
}
```

**Правило:** Всегда устанавливай `box-sizing: border-box` глобально.

---

### 5. Flexbox и Grid - правильный выбор
**Принцип:** Используй Flexbox для одномерных layout, Grid для двумерных.

**Почему:**
- Каждый инструмент для своей задачи
- Лучшая производительность
- Более семантичный код

**Практика:**
```css
/* ✅ Хорошо - Flexbox для одномерного layout */
.navbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* ✅ Хорошо - Grid для двумерного layout */
.grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
}

/* ❌ Плохо - Grid для простого одномерного layout */
.navbar {
  display: grid;
  grid-template-columns: 1fr 1fr;
  /* Избыточно, Flexbox проще */
}
```

**Правило:** Flexbox для одномерных layout, Grid для двумерных.

---

### 6. Mobile First - всегда
**Принцип:** Пиши стили для мобильных устройств, затем расширяй для больших экранов.

**Почему:**
- Большинство пользователей на мобильных
- Меньший CSS для мобильных (быстрее загрузка)
- Прогрессивное улучшение

**Практика:**
```css
/* ✅ Хорошо - Mobile First */
.container {
  padding: 1rem;
  font-size: 14px;
}

@media (min-width: 768px) {
  .container {
    padding: 2rem;
    font-size: 16px;
  }
}

@media (min-width: 1024px) {
  .container {
    padding: 3rem;
    font-size: 18px;
  }
}

/* ❌ Плохо - Desktop First */
.container {
  padding: 3rem;
  font-size: 18px;
}

@media (max-width: 1023px) {
  .container {
    padding: 2rem;
    font-size: 16px;
  }
}
```

**Правило:** Всегда начинай с мобильных стилей, затем добавляй media queries для больших экранов.

---

### 7. CSS Variables (Custom Properties)
**Принцип:** Используй CSS переменные для цветов, размеров, значений которые могут меняться.

**Почему:**
- Централизованное управление (Single Source of Truth)
- Темизация (легко менять тему)
- Динамические значения (можно менять через JS)
- Следует принципу Explicit Dependencies

**Практика:**
```css
/* ✅ Хорошо - CSS переменные */
:root {
  --color-primary: #3b82f6;
  --color-secondary: #8b5cf6;
  --spacing-unit: 0.5rem;
  --border-radius: 0.5rem;
}

.button {
  background-color: var(--color-primary);
  padding: calc(var(--spacing-unit) * 2);
  border-radius: var(--border-radius);
}

/* Темная тема */
[data-theme="dark"] {
  --color-primary: #60a5fa;
  --color-secondary: #a78bfa;
}

/* ❌ Плохо - хардкод значений */
.button {
  background-color: #3b82f6;
  padding: 1rem;
  border-radius: 0.5rem;
}
```

**Правило:** Все значения, которые могут меняться, должны быть CSS переменными.

---

### 8. Избегай !important
**Принцип:** Используй `!important` только в крайних случаях (утилиты, переопределение библиотек).

**Почему:**
- Ломает каскадность
- Сложно переопределять
- Признак проблем со специфичностью
- Следует принципу Explicit Dependencies

**Практика:**
```css
/* ✅ Хорошо - правильная специфичность */
.button.button--primary {
  background-color: blue;
}

/* ⚠️ Допустимо - утилитарные классы */
.hidden {
  display: none !important;
}

/* ❌ Плохо - !important везде */
.button {
  background-color: blue !important;
  color: white !important;
  padding: 10px !important;
}
```

**Правило:** `!important` только для утилитарных классов или переопределения библиотек. В остальных случаях - правильная специфичность.

---

### 9. Производительность - простые селекторы
**Принцип:** Используй простые селекторы. Избегай глубокой вложенности.

**Почему:**
- Быстрее парсинг
- Лучшая производительность рендеринга
- Легче поддерживать
- Следует принципу KISS

**Практика:**
```css
/* ✅ Хорошо - простые селекторы */
.card { }
.card__title { }
.card__content { }

/* ❌ Плохо - сложные селекторы */
div.container > ul.list > li.item > a.link { }
/* Медленнее, сложнее поддерживать */
```

**Правило:** Простые селекторы лучше сложных. Один класс лучше вложенности.

---

### 10. Организация CSS - структура файлов
**Принцип:** Организуй CSS по функциональности. Используй методологию (BEM, SMACSS, ITCSS).

**Практика:**
```
styles/
├── base/
│   ├── reset.css
│   ├── typography.css
│   └── variables.css
├── components/
│   ├── button.css
│   ├── card.css
│   └── form.css
├── layout/
│   ├── header.css
│   ├── footer.css
│   └── grid.css
└── utilities/
    ├── spacing.css
    └── display.css
```

**Правило:** Организуй CSS по функциональности. Один файл = одна ответственность.

---

### 11. Accessibility - доступность
**Принцип:** Всегда учитывай доступность при написании CSS.

**Практика:**
```css
/* ✅ Хорошо - доступность */
.button {
  /* Минимальный размер кликабельной области */
  min-height: 44px;
  min-width: 44px;
  
  /* Focus состояния */
  outline: 2px solid transparent;
  outline-offset: 2px;
}

.button:focus-visible {
  outline: 2px solid var(--color-focus);
}

/* Контрастность текста */
.text {
  color: #000;
  background: #fff;
  /* Контрастность минимум 4.5:1 для обычного текста */
}

/* Не скрывай контент от скринридеров */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border-width: 0;
}
```

**Правило:** Всегда учитывай доступность: focus states, контрастность, размеры кликабельных областей.

---

### 12. Анимации - transform и opacity
**Принцип:** Используй `transform` и `opacity` для анимаций. Они не вызывают reflow/repaint.

**Почему:**
- Лучшая производительность
- Плавные анимации (60fps)
- Не блокируют основной поток
- Следует принципу Performance First

**Практика:**
```css
/* ✅ Хорошо - производительные свойства */
.element {
  transform: translateX(0);
  opacity: 1;
  transition: transform 0.3s ease, opacity 0.3s ease;
}

.element:hover {
  transform: translateX(10px);
  opacity: 0.8;
}

/* ❌ Плохо - свойства вызывающие reflow */
.element {
  left: 0;
  width: 100px;
  transition: left 0.3s, width 0.3s;
}

.element:hover {
  left: 10px;
  width: 200px;
  /* Вызывает reflow, медленнее */
}
```

**Правило:** Для анимаций используй `transform` и `opacity`. Избегай свойств, которые вызывают reflow (width, height, left, top, margin, padding).

---

### 13. Z-index - система значений
**Принцип:** Используй систему для z-index. Избегай магических чисел.

**Почему:**
- Предсказуемость слоёв
- Легче поддерживать
- Избежание конфликтов
- Следует принципу Explicit Dependencies

**Практика:**
```css
/* ✅ Хорошо - система z-index */
:root {
  --z-base: 1;
  --z-dropdown: 100;
  --z-sticky: 200;
  --z-fixed: 300;
  --z-modal-backdrop: 400;
  --z-modal: 500;
  --z-popover: 600;
  --z-tooltip: 700;
}

.modal {
  z-index: var(--z-modal);
}

/* ❌ Плохо - магические числа */
.modal {
  z-index: 9999; /* Почему именно 9999? */
}
```

**Правило:** Используй CSS переменные для z-index. Определи систему значений.

---

### 14. Нормализация и Reset
**Принцип:** Используй normalize.css или reset.css для единообразия между браузерами.

**Почему:**
- Убирает различия между браузерами
- Предсказуемое поведение
- Меньше багов
- Следует принципу Fail Fast

**Практика:**
```css
/* ✅ Хорошо - normalize.css подход */
/* Сохраняет полезные дефолты, исправляет баги */

/* Или reset.css подход */
/* Полный сброс, начинаешь с нуля */

/* Или современный подход - только нужные сбросы */
*,
*::before,
*::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}
```

**Правило:** Используй normalize.css или минимальный reset для единообразия.

---

### 15. Избегай inline стилей
**Принцип:** Используй классы вместо inline стилей, кроме динамических значений.

**Почему:**
- Переиспользование
- Поддержка (стили в одном месте)
- Разделение concerns
- Следует принципу DRY

**Практика:**
```tsx
// ✅ Хорошо - классы для статических стилей
<div className="card card--highlighted">Контент</div>

// ✅ Хорошо - inline для динамических значений
<div 
  className="card"
  style={{ 
    transform: `translateX(${offset}px)`,
    opacity: isVisible ? 1 : 0 
  }}
>
  Контент
</div>

// ❌ Плохо - inline для всего
<div style={{ padding: '1rem', background: 'blue', color: 'white' }}>
  Контент
</div>
```

**Правило:** Статические стили - классы. Динамические значения - inline styles.

---

## 🎯 ЧЕК-ЛИСТ НАПИСАНИЯ CSS

- [ ] Использую кастомный CSS только когда Tailwind не может решить задачу
- [ ] Использую BEM для именования классов
- [ ] Понимаю специфичность и контролирую её (низкая специфичность)
- [ ] Использую `border-box` для box-sizing
- [ ] Правильный выбор между Flexbox и Grid
- [ ] Mobile First подход для responsive
- [ ] CSS переменные для цветов и размеров
- [ ] Избегаю `!important` (кроме утилит)
- [ ] Простые селекторы, не глубокая вложенность
- [ ] CSS организован по структуре/функциональности
- [ ] Учитываю accessibility (focus, контрастность, размеры)
- [ ] Анимации на `transform` и `opacity`
- [ ] Система для z-index (CSS переменные)
- [ ] Нормализация/reset для кроссбраузерности
- [ ] Избегаю inline стилей (кроме динамических значений)

---

## 📚 Полезные ресурсы

- [MDN CSS Documentation](https://developer.mozilla.org/en-US/docs/Web/CSS)
- [CSS Tricks](https://css-tricks.com/) - статьи и руководства
- [Can I Use](https://caniuse.com/) - поддержка CSS свойств в браузерах
- [BEM Methodology](https://en.bem.info/methodology/)
- [A Complete Guide to Flexbox](https://css-tricks.com/snippets/css/complete-guide-grid/)
- [A Complete Guide to Grid](https://css-tricks.com/snippets/css/complete-guide-grid/)

---

## 🚀 Современные CSS возможности

### Container Queries
```css
.card {
  container-type: inline-size;
}

@container (min-width: 400px) {
  .card__title {
    font-size: 1.5rem;
  }
}
```

### :has() селектор
```css
.card:has(.card__image) {
  display: grid;
  grid-template-columns: 200px 1fr;
}
```

### CSS Nesting
```css
.card {
  padding: 1rem;
  
  &__title {
    font-size: 1.5rem;
  }
  
  &:hover {
    transform: scale(1.05);
  }
}
```

### Logical Properties
```css
/* ✅ Хорошо - логические свойства (поддержка RTL) */
.element {
  margin-inline-start: 1rem;
  margin-inline-end: 1rem;
  padding-block: 1rem;
}

/* Вместо */
.element {
  margin-left: 1rem;
  margin-right: 1rem;
  padding-top: 1rem;
  padding-bottom: 1rem;
}
```

---

## ⚠️ ВАЖНО: СООТНОШЕНИЕ С TAILWIND

В этом проекте:
- **Tailwind CSS** - основной инструмент стилизации (см. `tailwind.md`)
- **Кастомный CSS** - только когда Tailwind не может решить задачу

Если возникает вопрос "Tailwind или кастомный CSS?", ответ: **Tailwind** (если не доказано обратное).
